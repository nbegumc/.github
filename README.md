# Central GitHub Configuration

This repository contains shared configuration for all repositories.

Includes:

- Reusable CI workflow (code quality + tests) using uv
- Issue templates
- Pull request template
- Dependabot for automatic dependency updates